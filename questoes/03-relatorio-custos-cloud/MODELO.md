**Modelo:** GPT-4o (OpenAI) — escolhido para **relatório executivo** com tabelas Markdown, números e ordenação por impacto.

**Execução neste workspace:** saída produzida pelo agente Cursor a partir do `PROMPT.txt` (sem `OPENAI_API_KEY` no shell). Para reprodução fiel com a API: `python3 scripts/run_openai.py questoes/03-relatorio-custos-cloud/PROMPT.txt`.
