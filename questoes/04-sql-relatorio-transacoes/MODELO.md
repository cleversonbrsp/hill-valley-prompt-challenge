**Modelo:** GPT-4o mini (OpenAI) — escolhido por **boa aderência a SQL** e formato “apenas o fence”, com custo baixo para iteração.

**Execução neste workspace:** saída abaixo foi gerada pelo agente Cursor a partir do `PROMPT.txt` (sem `OPENAI_API_KEY`). Reprodução API: `python3 scripts/run_openai.py questoes/04-sql-relatorio-transacoes/PROMPT.txt` (ajuste `OPENAI_MODEL` se quiser `gpt-4o`).
