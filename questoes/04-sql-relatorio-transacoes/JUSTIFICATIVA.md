# Justificativa — T-A-G (Questão 04)

**Task:** descreve o relatório desejado (filtros, janela temporal com fuso, agrupamentos, métricas e ordenação).

**Action:** instrui explicitamente a montar **uma única query** Postgres usando só `transactions` e regras de negócio (status/categorias/janela).

**Goal:** exige saída **somente** como bloco `sql`, reduzindo a chance de o modelo gastar tokens com explicação fora do artefato.
