**Modelo:** GPT-4o (OpenAI) — escolhido para **HCL** multi-arquivo com dependências entre buckets (logging) e contratos de variáveis (`description` + `type`).

**Reprodução API OpenAI:** `python3 scripts/run_openai.py questoes/06-modulo-terraform-s3/PROMPT.txt`
