Before:
Here is the current Kubernetes Deployment manifest for chronos-api in namespace production (legacy). Treat it as the exact "before" state:

apiVersion: apps/v1
kind: Deployment
metadata:
  name: chronos-api
  namespace: production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chronos-api
  template:
    metadata:
      labels:
        app: chronos-api
    spec:
      containers:
      - name: api
        image: chronos-api:latest
        ports:
        - containerPort: 8080
        env:
        - name: DB_PASSWORD
          value: "P@ssw0rd2023!"
        - name: JWT_SECRET
          value: "hvt-jwt-prod-secret"

After:
Modernize this to current HVT production standards:
- High availability appropriate for an API behind load balancers (no single replica unless justified; prefer >=3 pods and safe rolling updates).
- Use a versioned container image tag (NOT latest). Use tag v2.48.0 unless you must keep another tag—explain only if you change it.
- Remove plaintext secrets from manifests. Use Kubernetes Secret references via secretKeyRef (you may include a separate Secret manifest ONLY as an example with placeholder values clearly marked DO NOT COMMIT REAL SECRETS).
- Add CPU/memory requests and limits.
- Add liveness and readiness HTTP probes on port 8080 using paths `/livez` and `/readyz` (if you truly must change paths, keep HTTP probes and justify inside YAML comments only).
- Add Pod-level and container-level securityContext: non-root, readOnlyRootFilesystem where feasible, drop ALL caps, allowPrivilegeEscalation false, seccompProfile RuntimeDefault.
- Add standard labels: app, component, version.
- Add a sensible rollingUpdate strategy maxUnavailable/maxSurge.
- Optional but preferred if it fits cleanly: PodDisruptionBudget manifest for minAvailable=2.

Bridge:
You are the on-call Kubernetes engineer updating GitOps. Transform the Before manifest into production-grade YAML. Keep the same Deployment name and namespace unless a change is strictly required (if required, comment why).

Output format:
Return ONLY YAML in one or more ```yaml fenced blocks:
1) deployment.yaml (and if needed, a second block) pdb.yaml
2) optional secret-example.yaml clearly labeled as example placeholders
No prose outside fences.
